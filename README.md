# G-Engine Compile Tool

Simple CLI tool to install haxelibs for G-Engine installed via Haxelib.

## Installation

`haxelib git compiletool https://github.com/gteamfnf/G-Engine-CompileTool.git` 

## Running

`haxelib run compiletool`
